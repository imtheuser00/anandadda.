const defaults = { brandName: 'Anand Adda', heroText: 'Fresh everyday styles, handpicked for your street, your plans and your personality.', offerTitle: 'Your next favourite fit is waiting.', offerText: 'Add your sale, festive offer or new-arrival announcement here.', shopDescription: 'Anand Adda is a welcoming local clothing shop where you can discover easy, confident fashion in person.', knownFor: 'Fresh local styles', perfectFor: 'Youthful everyday fashion', hours: '10:00 AM – 9:00 PM', address: 'Add full shop address from the owner panel.', phone: '', whatsapp: '', instagram: '@yourshop', mapUrl: 'https://maps.google.com' };
const saved = { ...defaults, ...JSON.parse(localStorage.getItem('storeData') || '{}') };
const form = document.getElementById('settingsForm');
Object.entries(saved).forEach(([key,value]) => { if (form.elements[key]) form.elements[key].value = value; });
form.addEventListener('submit', event => { event.preventDefault(); const values = Object.fromEntries(new FormData(form)); localStorage.setItem('storeData', JSON.stringify({ ...saved, ...values })); const button = document.querySelector('button[type="submit"]'); button.textContent = 'Saved ✓'; setTimeout(() => button.textContent = 'Save changes', 1800); });
